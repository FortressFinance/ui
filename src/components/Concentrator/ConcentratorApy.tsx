import { FC } from "react"

export const VaultApy: FC = () => {
  //const poolId = useVaultPoolId(props)
  // const totalApy = useConcentratorApy()

  // return (
  //   <Skeleton isLoading={totalApy.isLoading}>
  //     {formatPercentage(totalApy.data)}
  //   </Skeleton>
  // )
  return null
}
